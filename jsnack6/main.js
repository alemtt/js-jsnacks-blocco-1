/*const number = parseInt(prompt('inserisci qui un numero'))

const cube = number * number * number
console.log(cube);
*/
for (let i = 0; i < 5; i++) {
    const number = parseInt(prompt('inserisci qui un numero'))
    const cube = number * number * number
    console.log(cube);
}

